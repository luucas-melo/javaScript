// CÓDIGO NÃO EXECUTÁVEL

// Prodecural

processamento(valor1, valor2, valor3)

//OO
objeto = {
    valor1, 
    valor2,
    valor3,
    processamento() {
        // ...
    }
}

objeto.processamento() // FOco passou a ser o objeto

// Principios importantes:

// 1. Abstração
/**
 Representa uma entiddade do mundo real
 */

// 2. Encapsulamento 
/**
 Ténica para que detalhes internos do funcionamento dos 
 métodos de uma classe fiquem ocultos para os objetos.
 */

 // 3. Herança 
 /**
  Forma de compartilhar atributos e métodos com classes
 */

 // 4. Polimorfismo
 /**
    *Signfica várias formas, indica que vários objetos podem ter 
    comportamentos diferentes, mesmo sendo da mesma classe

    *Sobrecarga: declaração do msm método, porém com assinaturas diferentes.
    *Sobrescrita: Só é possível em classes herdadas, e sobrescreve um método
  */